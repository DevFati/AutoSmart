<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    android:elevation="4dp"
    android:foreground="?attr/selectableItemBackground"
    android:padding="8dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Vehículo y matrícula -->
        <TextView
            android:id="@+id/tvMaintVehicle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🚗 Marca Modelo (Año)"
            android:textStyle="bold"
            android:textColor="@color/purple_700"
            android:textSize="16sp"/>

        <TextView
            android:id="@+id/tvMaintPlate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvMaintVehicle"
            android:paddingTop="4dp"
            android:text="🪪 ABC1234"
            android:textSize="14sp"/>

        <!-- Fecha, tipo y coste -->
        <TextView
            android:id="@+id/tvMaintDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvMaintPlate"
            android:paddingTop="4dp"
            android:text="🗓 2025-05-04"
            android:textSize="14sp"/>

        <TextView
            android:id="@+id/tvMaintType"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignBaseline="@id/tvMaintDate"
            android:layout_toEndOf="@id/tvMaintDate"
            android:paddingStart="16dp"
            android:text="🔧 Cambio de aceite"
            android:textSize="14sp"/>

        <TextView
            android:id="@+id/tvMaintCost"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@id/tvMaintDate"
            android:paddingTop="4dp"
            android:text="💶 49.99"
            android:textSize="14sp"/>

        <!-- Botones de acción -->
        <ImageButton
            android:id="@+id/btnEdit"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_alignParentEnd="true"
            android:contentDescription="Editar"
            android:src="@drawable/ic_edit"
            android:background="?attr/selectableItemBackgroundBorderless"/>

        <ImageButton
            android:id="@+id/btnDelete"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:layout_toStartOf="@id/btnEdit"
            android:layout_alignTop="@id/btnEdit"
            android:contentDescription="Eliminar"
            android:src="@drawable/ic_delete"
            android:background="?attr/selectableItemBackgroundBorderless"/>

    </RelativeLayout>
</androidx.cardview.widget.CardView>
